version: 2

models:
  - name: int_m3__sq_demo_source2
    description: "Recovered SQ_demo_source2 source qualifier output after its SQL override filter."
    columns:
      - name: Member_Type_Code
        description: "RECOVERED: SQ_demo_source2 SQL override requires Member_Type_Code IS NOT NULL."
        tests: [not_null]
  - name: int_m3__exptrans
    description: "Recovered EXPTRANS pass-through with the relationship-label ABORT represented by an error test."
    columns:
      - name: o_Relationship_to_Subscriber_Code_Label
        description: "RECOVERED: EXPTRANS label port invokes ABORT on NULL; DECISION: retain the pass-through value and enforce the second ERROR-severity guard here."
        tests: [not_null]
  - name: int_m3__rtr_newgroup1
    description: "Recovered RTRTRANS NEWGROUP1 rows where Social_Security_Number IS NULL."
  - name: int_m3__rtr_newgroup2
    description: "Recovered RTRTRANS NEWGROUP2 rows where Social_Security_Number IS NOT NULL."
    columns:
      - name: Social_Security_Number
        description: "RECOVERED: NEWGROUP2 condition requires Social_Security_Number IS NOT NULL."
        tests: [not_null]
  - name: int_m3__abort_fixture
    description: "Ephemeral recovered abort row fixture for demonstrating the EXPTRANS hard failure."
    config:
      materialized: ephemeral
