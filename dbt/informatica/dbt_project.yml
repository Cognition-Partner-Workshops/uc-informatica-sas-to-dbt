name: informatica_migration
version: "1.0.0"
config-version: 2
profile: informatica

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  informatica_migration:
    +materialized: table
    staging:
      +materialized: view
    intermediate:
      +materialized: view
