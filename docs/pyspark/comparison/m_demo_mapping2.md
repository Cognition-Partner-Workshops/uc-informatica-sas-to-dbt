| Mapping | Transformation | Port / Object | XML line | Original Informatica code (verbatim) | Converted PySpark code (file:lines + snippet) | Confidence | Reason & closing action |
|---|---|---|---:|---|---|---|---|
| m_demo_mapping2 | SQ_demo_source1 | Sql Query | 151 | `` (empty) | `m_demo_mapping2.py: no SQL override; source is consumed from ctx.sources` | MEDIUM | A non-empty override would alter source rows, but parity on seed data cannot catch an implementation that silently injects SQL; closing action: reconcile any future non-empty XML override. |
| m_demo_mapping2 | EXPTRANS | LEAD_CO_MNE | 164 | `LEAD_CO_MNE` | `m_demo_mapping2.py:32` — `F.col("source.LEAD_CO_MNE")` | HIGH | Direct source pass-through is unambiguous and affects target parity. |
| m_demo_mapping2 | EXPTRANS | BRANCH_CO_MNE | 165 | `BRANCH_CO_MNE` | `m_demo_mapping2.py:33` — `F.col("source.BRANCH_CO_MNE")` | HIGH | Direct source pass-through is unambiguous and affects target parity. |
| m_demo_mapping2 | EXPTRANS | MIS_DATE | 166 | `MIS_DATE` | `m_demo_mapping2.py:34` — `F.col("source.MIS_DATE")` | HIGH | Direct source pass-through is unambiguous and affects target parity. |
| m_demo_mapping2 | EXPTRANS | ID | 167 | `ID` | `m_demo_mapping2.py:35` — `F.col("source.ID")` | HIGH | Direct source pass-through is unambiguous and affects target parity. |
| m_demo_mapping2 | EXPTRANS | DESCRIPTION | 168 | `DESCRIPTION` | `m_demo_mapping2.py:36` — `F.col("source.DESCRIPTION")` | HIGH | Direct source pass-through is unambiguous and affects target parity. |
| m_demo_mapping2 | EXPTRANS | SHORT_NAME | 169 | `SHORT_NAME` | `m_demo_mapping2.py:37` — `F.col("source.SHORT_NAME")` | HIGH | Direct source pass-through is unambiguous and affects target parity. |
| m_demo_mapping2 | EXPTRANS | Key | 170 | `Key` | `m_demo_mapping2.py:38` — `F.col("lookup.Key")` after `cast("double")` at :19 | HIGH | Lookup key type and value affect both target outputs; a wrong cast or row changes parity. |
| m_demo_mapping2 | EXPTRANS | LEAD_CO_MNE1 | 171 | `LEAD_CO_MNE1` | `m_demo_mapping2.py:39` — `F.col("lookup.LEAD_CO_MNE")` | HIGH | This lookup port feeds MD5_src and therefore the Update decision; a wrong value changes target parity. |
| m_demo_mapping2 | EXPTRANS | BRANCH_CO_MNE1 | 172 | `BRANCH_CO_MNE1` | `m_demo_mapping2.py:40` — `F.col("lookup.BRANCH_CO_MNE")` | NOT MIGRATED | This lookup port reaches only dead router payloads; a wrong conversion would not alter targets and controls cannot catch it. Closing action: migrate if a target connector is added. |
| m_demo_mapping2 | EXPTRANS | MIS_DATE1 | 173 | `MIS_DATE1` | `m_demo_mapping2.py:41` — `F.col("lookup.MIS_DATE")` | NOT MIGRATED | This lookup port reaches only dead router payloads; a wrong conversion would not alter targets and controls cannot catch it. Closing action: migrate if a target connector is added. |
| m_demo_mapping2 | EXPTRANS | DESCRIPTION1 | 174 | `DESCRIPTION1` | `m_demo_mapping2.py:42` — `F.col("lookup.DESCRIPTION")` | NOT MIGRATED | This lookup port is intentionally not used by targets; a wrong conversion would be invisible to parity and controls. Closing action: migrate if a connector is added. |
| m_demo_mapping2 | EXPTRANS | SHORT_NAME1 | 175 | `SHORT_NAME1` | `m_demo_mapping2.py:43` — `F.col("lookup.SHORT_NAME")` | NOT MIGRATED | This lookup port reaches no target; a wrong conversion would be invisible to parity and controls. Closing action: migrate if a connector is added. |
| m_demo_mapping2 | EXPTRANS | New_Flag | 176 | `IIF(ISNULL(Key),'Insert')` | `m_demo_mapping2.py:46` — `iif(isnull(F.col("Key")), F.lit("Insert"))` | HIGH | Two-argument IIF's NULL false branch controls insert routing and is parity-visible. |
| m_demo_mapping2 | EXPTRANS | MD5_src | 177 | `AES_DECRYPT(LEAD_CO_MNE1, SUBSTR(SHORT_NAME,1,3), 256)` | `m_demo_mapping2.py:48-53` — `aes_decrypt(F.col("LEAD_CO_MNE1"), substr(F.col("SHORT_NAME"), 1, 3), F.lit(256))` | LOW | A real AES-256 decrypt or NULL would be a different conversion, but any value not exactly the 32-char MD5 digest has the same Update outcome; a wrong value is not caught because the seed is degenerate. Closing action: obtain authoritative decrypt semantics/key and rerun parity; rejected alternatives are real AES-256 decrypt and NULL. |
| m_demo_mapping2 | EXPTRANS | MD5_tgt | 178 | `MD5(LEAD_CO_MNE || BRANCH_CO_MNE || MIS_DATE || DESCRIPTION || SHORT_NAME)` | `m_demo_mapping2.py:55-64` — `md5(concat(...))` | HIGH | The digest participates in the reproduced defect and is parity-visible. |
| m_demo_mapping2 | EXPTRANS | Changed_Flag | 179 | `IIF(NOT ISNULL(Key) AND  (MD5_tgt != MD5_src),'Update')` | `m_demo_mapping2.py:66-72` — `iif(not_(isnull(F.col("Key"))) & (F.col("MD5_tgt") != F.col("MD5_src")), F.lit("Update"))` | HIGH | The incomparable AES/MD5 values flag every matched row; changing it changes UPD parity. |
| m_demo_mapping2 | EXPTRANS | o_CREATED_BY | 180 | `'IDWUSER'` | `m_demo_mapping2.py:73` — `F.lit("IDWUSER")` | MEDIUM | Constant output is weakly exercised; a wrong constant would alter populated INS rows but limited seed controls may miss it. Closing action: validate against an independent production sample. |
| m_demo_mapping2 | EXPTRANS | o_CREATED_TIME | 181 | `SYSDATE` | `m_demo_mapping2.py:74` — `ctx.sysdate` | MEDIUM | Pinned date is degenerate and cannot detect runtime-date misuse; wrong conversion would render a different date. Closing action: validate with a non-pinned business date. |
| m_demo_mapping2 | EXPTRANS | o_UPDATED_BY | 182 | `'IDWUSER'` | `m_demo_mapping2.py:75` — `F.lit("IDWUSER")` | MEDIUM | Constant output is weakly exercised; wrong output could evade controls on sparse update assertions. Closing action: validate against an independent production sample. |
| m_demo_mapping2 | EXPTRANS | o_UPDATED_TIME | 183 | `SYSDATE` | `m_demo_mapping2.py:76` — `ctx.sysdate` | MEDIUM | Pinned date is degenerate; a timestamp or runtime date would be wrong but seed parity alone is weak. Closing action: validate with a non-pinned business date and date-format assertion. |
| m_demo_mapping2 | LKPTRANS | lookup condition | 286 | `ID = ID1` | `m_demo_mapping2.py:25-29` — `source.ID == lookup.ID` in a left join | HIGH | The lookup condition determines matched versus inserted rows and is parity-visible. |
| m_demo_mapping2 | LKPTRANS | Lookup policy on multiple match | 285 | `Use Any Value` | `m_demo_mapping2.py:16-23` — `row_number().over(Window.partitionBy("ID").orderBy(SRC_ORDINAL.desc()))` | LOW | XML does not determine which duplicate wins. Rejected alternatives: highest `Key` (coincidentally selects the same row here) and lowest `SRC_ORDINAL` (selects Key 2 and fails parity). Closing action: obtain a confirmed platform tie-break rule or additional duplicate fixtures. |
| m_demo_mapping2 | RTRTRANS | Insert group | 188 | `New_Flag='Insert'` | `m_demo_mapping2.py:78` — `transformed.where(F.col("New_Flag") == F.lit("Insert"))` | HIGH | Independent matching is parity-visible; a first-match router could lose rows. |
| m_demo_mapping2 | RTRTRANS | Update group | 190 | `Changed_Flag='Update'` | `m_demo_mapping2.py:79` — `transformed.where(F.col("Changed_Flag") == F.lit("Update"))` | HIGH | Independent matching is parity-visible; a first-match router could lose rows. |
| m_demo_mapping2 | RTRTRANS | DEFAULT1 group | 189 | `DEFAULT1` (no expression; default group) | `m_demo_mapping2.py:78-79` — only Insert and Update frames are written | NOT MIGRATED | DEFAULT1 has no outgoing connector. A wrong conversion would invent an output target, while controls cannot catch absent dead data. Closing action: add a connector before migrating DEFAULT1. |
| m_demo_mapping2 | SEQTRANS | CURRVAL | 316 | `CURRVAL` | `m_demo_mapping2.py: no output column; only NEXTVAL is assigned` | NOT MIGRATED | CURRVAL has no outgoing connector. A wrong conversion would invent a sequence output that controls cannot observe. Closing action: migrate if a connector is added. |
| m_demo_mapping2 | SEQTRANS | generator state | 317-318, 320 | `Start Value=0`; `Increment By=1`; `Current Value=57` | `m_demo_mapping2.py:80-83` — `56 + row_number().over(Window.orderBy(SRC_ORDINAL))` | HIGH | Insert keys are parity-visible and source order is explicit. |
| m_demo_mapping2 | UPDTRANS | Update Strategy Expression | 341 | `DD_UPDATE` | `m_demo_mapping2.py:97-106` — UPD frame writes forwarded rows to `demo_target1_UPD` | HIGH | Every forwarded update is written to the UPD instance; no CSV/warehouse upsert is modeled. |
| m_demo_mapping2 | EXPTRANS | ERROR defaults | 176, 179-183 | `ERROR('transformation error')` | `functions.py:78-80` — `error` returns NULL; no expression raises | MEDIUM | Defaults never fire for these expressions; a raising implementation would abort instead, but seed parity cannot exercise the dormant path. Closing action: obtain a reject/error-row fixture and verify runner semantics. |
